function add_three_numbers(k,d,a){
    let add = k + d + a ;
    console.log("Sum", add);

}
add_three_numbers(4,10,5);

function multiply_two_numbers(r,h){
    let multiply = r * h ;
    console.log("Answer", multiply);
}
multiply_two_numbers(4,4)

 function divide_two_numbers(t,o){
    let divide = t / o ;
    console.log("Rem", divide);
 }
 divide_two_numbers(16,2)

 function subtarct_two_numbers(c,y){
    let subtract = c - y ;
    console.log("Num", subtract);
 }
 subtarct_two_numbers(5,3)

  function area_of_circle(d){
    let area = Math.PI * d * d ;
    console.log("fin " + area);
  }
  area_of_circle(4);

function circleArea(radius){
    return (Math.PI * radius * radius)
}
console.log(circleArea(9));